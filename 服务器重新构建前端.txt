# 重新构建前端以修复用户管理空白

## 🔍 1. 检查 dist 目录构建时间
```bash
ls -lh /var/www/activities_management/校园活动管理系统/dist/
```

## 🔧 2. 重新构建前端
```bash
cd /var/www/activities_management/校园活动管理系统
npm run build
```

## 🔍 3. 如果构建失败，检查错误
```bash
cd /var/www/activities_management/校园活动管理系统
npm install  # 确保依赖已安装
npm run build 2>&1 | tee build.log
```

## 🔄 4. 构建完成后，清除浏览器缓存
在浏览器中按 Ctrl+Shift+R 或 Ctrl+F5 强制刷新

## ⚡ 一键命令
```bash
cd /var/www/activities_management/校园活动管理系统 && \
echo "=== 当前构建时间 ===" && \
ls -lh dist/ | head -5 && \
echo "" && \
echo "=== 开始重新构建 ===" && \
npm run build && \
echo "✅ 构建完成！请清除浏览器缓存后刷新页面"
```

