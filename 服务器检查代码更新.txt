# 服务器检查代码是否已更新

## 🔍 检查服务器代码版本

### 1. 检查删除礼品函数是否使用正确的表名
```bash
cd /var/www/activities_management/backend/controllers
grep -A 5 "exports.deleteGift" giftController.js | grep "gift_items"
```

如果输出为空，说明代码还没更新。

### 2. 检查所有使用 gifts 表的地方
```bash
cd /var/www/activities_management/backend/controllers
grep -n "FROM gifts\|gifts WHERE" giftController.js
```

如果还有输出，说明代码还没更新。

### 3. 强制更新代码
```bash
cd /var/www/activities_management
git fetch origin
git reset --hard origin/main
pm2 restart all --update-env
```

### 4. 验证更新成功
```bash
cd /var/www/activities_management/backend/controllers
grep -A 3 "exports.deleteGift" giftController.js | head -10
```

应该看到 `gift_items` 而不是 `gifts`。

